[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "masters_thesis"
version = "0.1.0"

[tool.setuptools]
packages = ["lib", "lib.fs", "plotting"] # Explicitly tell it where the code is